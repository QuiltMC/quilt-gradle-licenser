plugins {
	id 'org.quiltmc.gradle.licenser'
	id 'java'
}

sourceSets {
	create("custom") {
		java {
			srcDir "src/custom/java"
		}
	}
}

license {
	rule rootProject.file("HEADER")
	include "**/*.java"
	include "**/*.kt"
	exclude sourceSets.getByName("custom")
}
